{
  "name": "pruna-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal web app for image & video generation/editing via the Pruna AI API, deployed on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
